<p id="notice"><%= notice %></p>

<h1>Historial de la Mascota</h1>

<table>
  <thead>
    <tr>
      <th>Weight</th>
      <th>Heigth</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @pet_histories.each do |pet_history| %>
      <tr>
        <td><%= pet_history.weight %></td>
        <td><%= pet_history.heigth %></td>
        <td><%= pet_history.description %></td>
        <td><%= link_to 'Ver', pet_history %></td>
        <td><%= link_to 'Editar', edit_pet_history_path(pet_history) %></td>
        <td><%= link_to 'Eliminar', pet_history, method: :delete, data: { confirm: '¿Realmente quieres eliminar esta historia?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pet History', new_pet_pet_history_url %>
